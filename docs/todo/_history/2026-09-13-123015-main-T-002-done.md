# 2026-09-13 12:30 - main - T-002 done

Decompose MainWindow.xaml into per-screen and per-panel UserControls: `review` -> `done`, `spec: reviewed` -> `amended`.

- Behaviour parity measured on the pre-T-002 build (`4998dde`) and the current one, each run inside its own exe:
  45 checks identical and passing, 0 binding errors in both, all 21 help popups identical.
- LFK-1 fixed: the nine controls no longer add unnamed nodes to the UI Automation tree (Control view identical);
  locked by a negative-controlled test. T-011 flagged.
- Accepted deviation: `MainWindow.xaml.cs` is 141 lines against a 130 budget, to keep the rule-18 invariant comment.
- Clears the `blocked-by` edge on T-003. T-003 still carries an open decision (D1 has no recommendation), so it stays blocked.
