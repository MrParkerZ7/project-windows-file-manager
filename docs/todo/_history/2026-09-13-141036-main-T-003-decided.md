# 2026-09-13 14:10 - main - T-003 decided

`todo-options T-003`: both forks answered by the user. `decision:` cleared, `spec: reviewed` -> `amended`.

- D1 = B: the two template slots are typed attached properties in `Helpers/BroadsheetSlots.cs`, not `Tag`.
- D2 = A: no chip style; `Broadsheet.CheckBox` only, as T-006 already renders it.
- Surfaced for reconciliation, not edited: T-004 names its own tab-strip slot (`ShellChrome.StripRightContent`) and style;
  T-006 still assumes the retired `HeaderedContentControl` + `Tag` header and `Bs*` keys; T-012 must record typed slots.
- T-003 is now proceedable.
